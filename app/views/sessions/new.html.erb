<h1>log_in</h1>

<%= form_with(url: login_path, scope: :session, local: true) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit 'Log in' %>
<% end %>

<p>New user? <%= link_to 'Sign_up now', signup_path %></p>
